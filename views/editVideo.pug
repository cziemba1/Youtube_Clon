extends layout/main

block content
    .form-container
        form(action=routes.videos, method="post")
            input(type="text", placeholder="Title", name="title")
            textarea(name="description", cols="30", rows="10", placeholder="Description")
            input(type="submit", placeholder="Upload Video", name="Update Video")
        a.form-container__link(href=routes.deleteVideo) Delete Video