extends layout/main
include mixins/videoBlock

block content
    .videos 
        each video in videosAll
            +videoBlock({
                title: video.title,
                description: video.description
            })