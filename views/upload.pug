extends layout/main

block content
    .form-container
        form(action=`/videos/${routes.upload}`, method="post")
            label(for="videos") Video File
            input(type="file", id="videos", name="video", required=true)
            input(type="text", placeholder="Title", name="title", required=true)
            textarea(name="description", cols="30", rows="10", placeholder="Description", required=true)
            input(type="submit", placeholder="Upload Video", value="Submit Video")